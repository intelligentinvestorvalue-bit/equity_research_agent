# Research job sync artifacts live here (git-tracked).
# Local SQLite remains in data/research.db (gitignored).
# Flow: complete a job → auto-export here → git commit/push → other env git pull → app startup imports.
